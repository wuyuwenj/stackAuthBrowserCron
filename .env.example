# Database
# For local development, use Prisma Postgres or any PostgreSQL database
DATABASE_URL="prisma+postgres://localhost:51213/?api_key=YOUR_PRISMA_API_KEY"

# Stack Auth Configuration
# Get from: https://app.stack-auth.com/ (create a new project)
NEXT_PUBLIC_STACK_PROJECT_ID="your_project_id"
NEXT_PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY="your_publishable_client_key"
STACK_SECRET_SERVER_KEY="your_secret_server_key"

# Browser Use API
# Get your API key from https://cloud.browser-use.com
BROWSER_USE_API_KEY="bu_your_api_key_here"

# Cron Secret (optional but recommended)
# Use this to secure your /api/run-due-tasks endpoint
# Generate a random string: openssl rand -base64 32
CRON_SECRET="your_secure_random_string_here"

# Stripe Configuration
# Get your keys from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY="sk_test_your_stripe_secret_key"
STRIPE_WEBHOOK_SECRET="whsec_your_webhook_secret"

# Stripe Price IDs (create products in Stripe Dashboard)
STRIPE_PRO_PRICE_ID="price_your_pro_plan_price_id"
STRIPE_PREMIUM_PRICE_ID="price_your_premium_plan_price_id"

# App URL (for Stripe redirects and email links)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Email Notifications (Resend)
# Sign up at https://resend.com and get your API key
# Optional: Verify your domain to send from custom email address
RESEND_API_KEY="re_your_resend_api_key"
RESEND_FROM_EMAIL="BrowserCron <notifications@yourdomain.com>"

# Node Environment
NODE_ENV="development"
